/**
 * A level consists of an array of rectangles.
 * It takes the upper left and lower right point of a rectangle.
 * [ p1.x, p1.y, p2.x, p2.y]
 */
export type Level = [ number,number,number, number ][]