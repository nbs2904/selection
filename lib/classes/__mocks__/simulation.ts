/* eslint-disable @typescript-eslint/no-unused-vars */
// * classes
import { Node } from "@classes/node";
import { Position } from "@classes/position";

export const updateNodePositionMock = function (node : Node, newPosition : Position) : boolean {
    return true;
};